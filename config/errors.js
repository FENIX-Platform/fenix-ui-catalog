/*global define*/
define(function ( ) {

    'use strict';

    var prefix = "";

    return {

        request : prefix + "request",
        empty_values : prefix + "empty_values",
        missing_container : prefix + "missing_container"

    };
});
